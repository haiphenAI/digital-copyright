"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.hmgetBuffer = hmgetBuffer;

var _hmget = require("./hmget");

var _buffer = _interopRequireDefault(require("../buffer"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function hmgetBuffer(key) {
  for (var _len = arguments.length, fields = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    fields[_key - 1] = arguments[_key];
  }

  var val = _hmget.hmget.apply(this, [key].concat(fields));

  return val.map(function (payload) {
    return payload ? (0, _buffer.default)(payload) : payload;
  });
}