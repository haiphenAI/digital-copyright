"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.psubscribe = psubscribe;

var _channelSubscription = require("../commands-utils/channel-subscription");

function psubscribe() {
  var _this = this;

  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  args.forEach(function (pattern) {
    if (!_this.patternChannels.instanceListeners) {
      _this.patternChannels.instanceListeners = new Map();
    }

    (0, _channelSubscription.subscribeToChannel)(_this, pattern, _this.patternChannels, true);
  });
  var numberOfSubscribedChannels = (0, _channelSubscription.getSubscribedChannels)(this, this.patternChannels).length;

  if (numberOfSubscribedChannels > 0) {
    this.subscriberMode = true;
  }

  return numberOfSubscribedChannels;
}